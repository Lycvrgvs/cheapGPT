{
  "schemaVersion": 1,
  "packageVersion": "1.4.0",
  "profiles": {
    "ultracheap": {
      "id": "ultracheap",
      "file": "ultracheap.md",
      "title": "UltraCheap",
      "account": "ChatGPT Plus",
      "difficulty": "simple tasks",
      "root": "Luna xHigh",
      "planner": "GPT-6 Astra-medium",
      "reviewer": "GPT-6 Astra-low",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns implementation and completion (preferred root: Luna xHigh); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new cycle, spawn one GPT-6 Astra-medium child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 5000 tokens or less there, solve the creative/problem-solving core with essential production-quality real code and exact mechanisms, describe repetitive or mechanical propagation as precise step-by-step instructions for Luna, edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-medium-informed `cheapgpt/plan.md` itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable and doing the mechanical adaptation and propagation itself; once relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-low implementation-read-only first reviewer attached to this root task with all essential necessary review context, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed mechanical instructions within roughly 5000 tokens, then have the root read the board in full, perform exactly one coherent correction or replan, and retest, reconsulting Astra-medium only if genuinely new architectural advice is still required; after either correction path, spawn exactly one fresh Astra-low terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap": {
      "id": "cheap",
      "file": "cheap.md",
      "title": "Cheap",
      "account": "ChatGPT Plus",
      "difficulty": "medium-hard tasks",
      "root": "Luna Max",
      "planner": "GPT-6 Astra-medium",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns implementation and completion (preferred root: Luna Max); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new cycle, spawn one GPT-6 Astra-medium child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 10000 tokens or less there, solve the creative/problem-solving core with essential production-quality real code and exact mechanisms, describe repetitive or mechanical propagation as precise step-by-step instructions for Luna, edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-medium-informed `cheapgpt/plan.md` itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable and doing the mechanical adaptation and propagation itself; once relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-medium implementation-read-only first reviewer attached to this root task with all essential necessary review context, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed mechanical instructions within roughly 10000 tokens, then have the root read the board in full, perform exactly one coherent correction or replan, and retest, reconsulting Astra-medium only if genuinely new architectural advice is still required; after either correction path, spawn exactly one fresh Astra-medium terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap-5x": {
      "id": "cheap-5x",
      "file": "cheap-5x.md",
      "title": "Cheap 5x",
      "account": "ChatGPT Pro 5x",
      "difficulty": "hardest tasks",
      "root": "Sol-high",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns implementation and completion (preferred root: Sol-high); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new cycle, spawn one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 15000 tokens or less there, solve the creative/problem-solving core with essential production-quality real code and exact mechanisms, describe repetitive or mechanical propagation as precise step-by-step instructions for Sol, edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-xhigh-informed `cheapgpt/plan.md` itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable and doing the mechanical adaptation and propagation itself; once relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-medium implementation-read-only first reviewer attached to this root task with all essential necessary review context, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed mechanical instructions within roughly 15000 tokens, then have the root read the board in full, perform exactly one coherent correction or replan, and retest, reconsulting Astra-xhigh only if genuinely new architectural advice is still required; after either correction path, spawn exactly one fresh Astra-medium terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap-20x": {
      "id": "cheap-20x",
      "file": "cheap-20x.md",
      "title": "Cheap 20x",
      "account": "ChatGPT Pro 20x",
      "difficulty": "hardest tasks",
      "root": "gpt-5.6-sol xhigh",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns implementation and completion (preferred root: gpt-5.6-sol xhigh); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new cycle, spawn one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 20000 tokens or less there, solve the creative/problem-solving core with essential production-quality real code and exact mechanisms, describe repetitive or mechanical propagation as precise step-by-step instructions for Sol, edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-xhigh-informed `cheapgpt/plan.md` itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable and doing the mechanical adaptation and propagation itself; once relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-medium implementation-read-only first reviewer attached to this root task with all essential necessary review context, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed mechanical instructions within roughly 20000 tokens, then have the root read the board in full, perform exactly one coherent correction or replan, and retest, reconsulting Astra-xhigh only if genuinely new architectural advice is still required; after either correction path, spawn exactly one fresh Astra-medium terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    }
  }
}
