{
  "schemaVersion": 1,
  "packageVersion": "1.5.0",
  "profiles": {
    "ultracheap": {
      "id": "ultracheap",
      "file": "ultracheap.md",
      "title": "UltraCheap",
      "account": "ChatGPT Plus",
      "difficulty": "simple tasks",
      "root": "luna-6-max",
      "planner": "GPT-6 Astra-medium",
      "reviewer": "GPT-6 Astra-low",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns implementation and completion (preferred root: luna-6-max); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new `cheapgpt-plan` cycle, spawn one GPT-6 Astra-medium child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 5000 tokens or less there, solve the creative/problem-solving core with essential production-quality real code and exact mechanisms, describe repetitive or mechanical propagation as precise step-by-step instructions for luna-6-max, edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-medium-informed `cheapgpt/plan.md` itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable and doing the mechanical adaptation and propagation itself; once relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-low implementation-read-only first reviewer attached to this root task with all essential necessary review context, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed mechanical instructions within roughly 5000 tokens, then have the root read the board in full, perform exactly one coherent correction or replan, and retest, reconsulting Astra-medium only if genuinely new architectural advice is still required; after either correction path, spawn exactly one fresh Astra-low terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap": {
      "id": "cheap",
      "file": "cheap.md",
      "title": "Cheap",
      "account": "ChatGPT Plus",
      "difficulty": "medium-hard tasks",
      "root": "sol-6-high",
      "planner": "GPT-6 Astra-medium",
      "reviewer": "GPT-6 Astra-low",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns judgment-driven implementation, integration, and completion (preferred root: sol-6-high); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new `cheapgpt-plan` cycle, spawn one GPT-6 Astra-medium child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 10000 tokens or less there, assign judgment-driven tasks such as architecture, core algorithms/logic, creative/visual decisions, and root-cause debugging to Sol and spec-bound mechanical tasks such as all required automated test-suite and spy implementation, schema/catalog wiring, scaffolding, and verification triage to Luna, and provide both models with detailed implementation instructions, plans, essential production-quality real code and exact mechanisms, precise mechanical steps, editable files/symbols, dependencies, handoff points, and completion checks; require Sol to settle substantive decisions in mixed tasks and supply frozen contracts/interfaces before Luna begins dependent work, reserve 100% of required automated test-suite and spy authoring or modification for Luna without expanding the required testing scope, and plan disjoint editable paths or explicit ownership transfers for shared files in the same worktree; Astra must edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements the judgment-driven tasks in the Astra-medium-informed `cheapgpt/plan.md` using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable, and delegates spec-bound mechanical tasks, including 100% of required automated test-suite and spy authoring or modification, to one luna-6-max native child chat attached to the root with `fork_turns: \"none\"` in the same project and same worktree; supply Luna with Astra's detailed instructions, plan, and relevant code together with Sol's frozen contracts/interfaces, exact ownership and editable paths/symbols, expected behavior, invariants, dependencies, and validation requirements before dependent work begins, allow independent work to proceed concurrently, and prevent overlapping edits through disjoint ownership or explicitly sequenced transfers; Luna implements the specified mechanical work, runs checks, triages failures against the specification, and returns judgment-dependent questions to Sol, while Sol owns architecture, core logic, creative/visual decisions, root-cause debugging, contract changes, integration, and acceptance; wait for Luna's actual terminal handoff using native `wait_agent(timeout_ms=1800000)` when supported, or the longest supported timeout otherwise, and repeat waits after nonterminal returns, read and preserve its changes, affected paths, test results, and blockers in the main thread, resolve incomplete assigned work with the same child, and archive its completed thread; when no mechanical work exists, record the empty allocation and omit the idle child; only after both Sol and Luna finish all assigned work, Luna's completed thread is archived, the combined work is integrated, and relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-low implementation-read-only first reviewer attached to this root task with all essential necessary context for reviewing the combined implementation, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; when Luna's allocation is empty, completion of Sol's work, integration, and relevant tests satisfies this checkpoint without creating a Luna thread; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed instructions assigning judgment-driven corrections to Sol and mechanical corrections to Luna within roughly 10000 tokens, then have the root read the board in full, coordinate exactly one coherent correction or replan, and retest, using one new luna-6-max child attached to the root with `fork_turns: \"none\"` in the same project and same worktree only when mechanical corrections are required, supplying the correction packet and updated frozen contracts/interfaces, preventing overlapping edits, waiting for actual completion, preserving the completed handoff in the main thread, and archiving the completed child thread, reconsulting Astra-medium only if genuinely new architectural advice is still required; after both models complete all assigned corrections, any correction-stage Luna thread is archived, the combined corrections are integrated, and relevant checks pass, spawn exactly one fresh Astra-low terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap-5x": {
      "id": "cheap-5x",
      "file": "cheap-5x.md",
      "title": "Cheap 5x",
      "account": "ChatGPT Pro 5x",
      "difficulty": "hardest tasks",
      "root": "sol-6-xhigh",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns judgment-driven implementation, integration, and completion (preferred root: sol-6-xhigh); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new `cheapgpt-plan` cycle, spawn one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 15000 tokens or less there, assign judgment-driven tasks such as architecture, core algorithms/logic, creative/visual decisions, and root-cause debugging to Sol and spec-bound mechanical tasks such as all required automated test-suite and spy implementation, schema/catalog wiring, scaffolding, and verification triage to Luna, and provide both models with detailed implementation instructions, plans, essential production-quality real code and exact mechanisms, precise mechanical steps, editable files/symbols, dependencies, handoff points, and completion checks; require Sol to settle substantive decisions in mixed tasks and supply frozen contracts/interfaces before Luna begins dependent work, reserve 100% of required automated test-suite and spy authoring or modification for Luna without expanding the required testing scope, and plan disjoint editable paths or explicit ownership transfers for shared files in the same worktree; Astra must edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements the judgment-driven tasks in the Astra-xhigh-informed `cheapgpt/plan.md` using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable, and delegates spec-bound mechanical tasks, including 100% of required automated test-suite and spy authoring or modification, to one luna-6-max native child chat attached to the root with `fork_turns: \"none\"` in the same project and same worktree; supply Luna with Astra's detailed instructions, plan, and relevant code together with Sol's frozen contracts/interfaces, exact ownership and editable paths/symbols, expected behavior, invariants, dependencies, and validation requirements before dependent work begins, allow independent work to proceed concurrently, and prevent overlapping edits through disjoint ownership or explicitly sequenced transfers; Luna implements the specified mechanical work, runs checks, triages failures against the specification, and returns judgment-dependent questions to Sol, while Sol owns architecture, core logic, creative/visual decisions, root-cause debugging, contract changes, integration, and acceptance; wait for Luna's actual terminal handoff using native `wait_agent(timeout_ms=1800000)` when supported, or the longest supported timeout otherwise, and repeat waits after nonterminal returns, read and preserve its changes, affected paths, test results, and blockers in the main thread, resolve incomplete assigned work with the same child, and archive its completed thread; when no mechanical work exists, record the empty allocation and omit the idle child; only after both Sol and Luna finish all assigned work, Luna's completed thread is archived, the combined work is integrated, and relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-medium implementation-read-only first reviewer attached to this root task with all essential necessary context for reviewing the combined implementation, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; when Luna's allocation is empty, completion of Sol's work, integration, and relevant tests satisfies this checkpoint without creating a Luna thread; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed instructions assigning judgment-driven corrections to Sol and mechanical corrections to Luna within roughly 15000 tokens, then have the root read the board in full, coordinate exactly one coherent correction or replan, and retest, using one new luna-6-max child attached to the root with `fork_turns: \"none\"` in the same project and same worktree only when mechanical corrections are required, supplying the correction packet and updated frozen contracts/interfaces, preventing overlapping edits, waiting for actual completion, preserving the completed handoff in the main thread, and archiving the completed child thread, reconsulting Astra-xhigh only if genuinely new architectural advice is still required; after both models complete all assigned corrections, any correction-stage Luna thread is archived, the combined corrections are integrated, and relevant checks pass, spawn exactly one fresh Astra-medium terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap-20x": {
      "id": "cheap-20x",
      "file": "cheap-20x.md",
      "title": "Cheap 20x",
      "account": "ChatGPT Pro 20x",
      "difficulty": "hardest tasks",
      "root": "sol-6-max",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns judgment-driven implementation, integration, and completion (preferred root: sol-6-max); apply the native-subagent and communication-board contracts, create or clear `cheapgpt/plan.md` for a new `cheapgpt-plan` cycle, spawn one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, then immediately use native `wait_agent(timeout_ms=1800000)` when supported and re-enter the wait after any timeout or other nonterminal return until the child's actual terminal result arrives; require Astra to remain repository-source read-only except for `cheapgpt/plan.md`, write a detailed plan of roughly 20000 tokens or less there, assign judgment-driven tasks such as architecture, core algorithms/logic, creative/visual decisions, and root-cause debugging to Sol and spec-bound mechanical tasks such as all required automated test-suite and spy implementation, schema/catalog wiring, scaffolding, and verification triage to Luna, and provide both models with detailed implementation instructions, plans, essential production-quality real code and exact mechanisms, precise mechanical steps, editable files/symbols, dependencies, handoff points, and completion checks; require Sol to settle substantive decisions in mixed tasks and supply frozen contracts/interfaces before Luna begins dependent work, reserve 100% of required automated test-suite and spy authoring or modification for Luna without expanding the required testing scope, and plan disjoint editable paths or explicit ownership transfers for shared files in the same worktree; Astra must edit no other path, implement/test/delegate nothing, and return exactly `PLAN_READY cheapgpt/plan.md` only after the board is complete; then read the board in full, establish the smallest proportional acceptance contract and mechanism that satisfy the actual task, and avoid redundantly re-solving Astra's concrete mechanism unless repository evidence requires adaptation; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements the judgment-driven tasks in the Astra-xhigh-informed `cheapgpt/plan.md` using the smallest correct mechanism that satisfies the acceptance contract and fits the repository, using Astra's core real code where applicable, and delegates spec-bound mechanical tasks, including 100% of required automated test-suite and spy authoring or modification, to one luna-6-max native child chat attached to the root with `fork_turns: \"none\"` in the same project and same worktree; supply Luna with Astra's detailed instructions, plan, and relevant code together with Sol's frozen contracts/interfaces, exact ownership and editable paths/symbols, expected behavior, invariants, dependencies, and validation requirements before dependent work begins, allow independent work to proceed concurrently, and prevent overlapping edits through disjoint ownership or explicitly sequenced transfers; Luna implements the specified mechanical work, runs checks, triages failures against the specification, and returns judgment-dependent questions to Sol, while Sol owns architecture, core logic, creative/visual decisions, root-cause debugging, contract changes, integration, and acceptance; wait for Luna's actual terminal handoff using native `wait_agent(timeout_ms=1800000)` when supported, or the longest supported timeout otherwise, and repeat waits after nonterminal returns, read and preserve its changes, affected paths, test results, and blockers in the main thread, resolve incomplete assigned work with the same child, and archive its completed thread; when no mechanical work exists, record the empty allocation and omit the idle child; only after both Sol and Luna finish all assigned work, Luna's completed thread is archived, the combined work is integrated, and relevant tests pass, apply the native-subagent and communication-board contracts and spawn one fresh native `fork_turns: \"none\"` Astra-medium implementation-read-only first reviewer attached to this root task with all essential necessary context for reviewing the combined implementation, immediately await it with `wait_agent(timeout_ms=1800000)` when supported and keep waiting after nonterminal returns, and require `PASS`, `FIX cheapgpt/plan.md`, or `REPLAN cheapgpt/plan.md`; when Luna's allocation is empty, completion of Sol's work, integration, and relevant tests satisfies this checkpoint without creating a Luna thread; `PASS` ends the loop immediately after the root verifies the contract and deletes `cheapgpt/plan.md`; for `FIX` or `REPLAN`, require Astra first to overwrite the board with concrete evidence, the corrected/replacement mechanism, essential real code for the creative core, and detailed instructions assigning judgment-driven corrections to Sol and mechanical corrections to Luna within roughly 20000 tokens, then have the root read the board in full, coordinate exactly one coherent correction or replan, and retest, using one new luna-6-max child attached to the root with `fork_turns: \"none\"` in the same project and same worktree only when mechanical corrections are required, supplying the correction packet and updated frozen contracts/interfaces, preventing overlapping edits, waiting for actual completion, preserving the completed handoff in the main thread, and archiving the completed child thread, reconsulting Astra-xhigh only if genuinely new architectural advice is still required; after both models complete all assigned corrections, any correction-stage Luna thread is archived, the combined corrections are integrated, and relevant checks pass, spawn exactly one fresh Astra-medium terminal final reviewer, await it the same way, and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED cheapgpt/plan.md` after it writes only concrete remaining blockers and missing evidence/guidance to the board; after final `PASS`, delete `cheapgpt/plan.md` and stop; after `UNRESOLVED`, read and retain the board, report the unresolved state, and stop; never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    }
  }
}
