{
  "schemaVersion": 1,
  "packageVersion": "1.3.0",
  "profiles": {
    "ultracheap": {
      "id": "ultracheap",
      "file": "ultracheap.md",
      "title": "UltraCheap",
      "account": "ChatGPT Plus",
      "difficulty": "simple tasks",
      "root": "Luna xHigh",
      "planner": "GPT-6 Astra-medium",
      "reviewer": "GPT-6 Astra-low",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns the plan (preferred root: Luna xHigh); apply the native-subagent contract and spawn and await one GPT-6 Astra-medium child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, ask it to advise only and never edit, implement, test, or delegate, keep its output concise and denseful, require it to always include minimal fundamental code snippets or pseudocode-quality kernels plus guidance showing Luna how to expand the proposed mechanism, then integrate all its advice into the final plan yourself and establish the smallest proportional acceptance contract and mechanism that satisfy the actual task; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-medium-informed plan itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository; once relevant tests pass, apply the native-subagent contract and spawn and await one fresh native `fork_turns: \"none\"` Astra-low read-only first reviewer attached to this root task with all essential necessary review context for `PASS`, `FIX`, or `REPLAN`, requiring it to apply the relevance gate, stop once it can determine the verdict, and omit optional or speculative findings; `PASS` ends the loop immediately; for `FIX`, group blocking findings by root cause, make one coherent correction, and retest; for `REPLAN`, stop patching, choose and implement one simpler proportionate mechanism, reconsulting Astra-medium only if genuinely new architectural advice is required, and retest; after either correction path, spawn exactly one fresh Astra-low terminal final reviewer and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED` with only concrete remaining blockers and any missing evidence or user guidance if they are not; after this final review, stop, marking the task complete on `PASS` or reporting the unresolved state on `UNRESOLVED`, and never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap": {
      "id": "cheap",
      "file": "cheap.md",
      "title": "Cheap",
      "account": "ChatGPT Plus",
      "difficulty": "medium-hard tasks",
      "root": "Luna Max",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns the plan (preferred root: Luna Max); apply the native-subagent contract and spawn and await one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, ask it to advise only and never edit, implement, test, or delegate, keep its output concise and denseful, require it to always include minimal fundamental code snippets or pseudocode-quality kernels plus guidance showing Luna how to expand the proposed mechanism, then integrate all its advice into the final plan yourself and establish the smallest proportional acceptance contract and mechanism that satisfy the actual task; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-xhigh-informed plan itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository; once relevant tests pass, apply the native-subagent contract and spawn and await one fresh native `fork_turns: \"none\"` Astra-medium read-only first reviewer attached to this root task with all essential necessary review context for `PASS`, `FIX`, or `REPLAN`, requiring it to apply the relevance gate, stop once it can determine the verdict, and omit optional or speculative findings; `PASS` ends the loop immediately; for `FIX`, group blocking findings by root cause, make one coherent correction, and retest; for `REPLAN`, stop patching, choose and implement one simpler proportionate mechanism, reconsulting Astra-xhigh only if genuinely new architectural advice is required, and retest; after either correction path, spawn exactly one fresh Astra-medium terminal final reviewer and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED` with only concrete remaining blockers and any missing evidence or user guidance if they are not; after this final review, stop, marking the task complete on `PASS` or reporting the unresolved state on `UNRESOLVED`, and never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap-5x": {
      "id": "cheap-5x",
      "file": "cheap-5x.md",
      "title": "Cheap 5x",
      "account": "ChatGPT Pro 5x",
      "difficulty": "hardest tasks",
      "root": "Sol-high",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns the plan (preferred root: Sol-high); apply the native-subagent contract and spawn and await one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, ask it to advise only and never edit, implement, test, or delegate, keep its output concise and denseful, require it to always include minimal fundamental code snippets or pseudocode-quality kernels plus guidance showing Sol how to expand the proposed mechanism, then integrate all its advice into the final plan yourself and establish the smallest proportional acceptance contract and mechanism that satisfy the actual task; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-xhigh-informed plan itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository; once relevant tests pass, apply the native-subagent contract and spawn and await one fresh native `fork_turns: \"none\"` Astra-medium read-only first reviewer attached to this root task with all essential necessary review context for `PASS`, `FIX`, or `REPLAN`, requiring it to apply the relevance gate, stop once it can determine the verdict, and omit optional or speculative findings; `PASS` ends the loop immediately; for `FIX`, group blocking findings by root cause, make one coherent correction, and retest; for `REPLAN`, stop patching, choose and implement one simpler proportionate mechanism, reconsulting Astra-xhigh only if genuinely new architectural advice is required, and retest; after either correction path, spawn exactly one fresh Astra-medium terminal final reviewer and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED` with only concrete remaining blockers and any missing evidence or user guidance if they are not; after this final review, stop, marking the task complete on `PASS` or reporting the unresolved state on `UNRESOLVED`, and never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    },
    "cheap-20x": {
      "id": "cheap-20x",
      "file": "cheap-20x.md",
      "title": "Cheap 20x",
      "account": "ChatGPT Pro 20x",
      "difficulty": "hardest tasks",
      "root": "gpt-5.6-sol xhigh",
      "planner": "GPT-6 Astra-xhigh",
      "reviewer": "GPT-6 Astra-medium",
      "planningMode": "PLANNING MODE: The current root remains the persistent root and owns the plan (preferred root: gpt-5.6-sol xhigh); apply the native-subagent contract and spawn and await one GPT-6 Astra-xhigh child attached to this root task with `fork_turns: \"none\"` and all essential necessary task context, ask it to advise only and never edit, implement, test, or delegate, keep its output concise and denseful, require it to always include minimal fundamental code snippets or pseudocode-quality kernels plus guidance showing Sol how to expand the proposed mechanism, then integrate all its advice into the final plan yourself and establish the smallest proportional acceptance contract and mechanism that satisfy the actual task; apply the relevance gate so only explicit contract requirements, concrete evidence from the changed or directly affected repository path, regressions introduced by the change, or necessary correctness, security, data-integrity, validation, accessibility, or compatibility safeguards may expand the work, omit hypothetical edge cases, generic robustness, future extensibility, stylistic cleanup, unrelated defects, and optional improvements, and after understanding the real flow stop at the first sufficient mechanism by preferring existing code, standard-library, native-platform, or already-installed capabilities before minimum necessary new code.",
      "implementationMode": "IMPLEMENTATION MODE: The same persistent root implements, tests, debugs, and fixes the Astra-xhigh-informed plan itself using the smallest correct mechanism that satisfies the acceptance contract and fits the repository; once relevant tests pass, apply the native-subagent contract and spawn and await one fresh native `fork_turns: \"none\"` Astra-medium read-only first reviewer attached to this root task with all essential necessary review context for `PASS`, `FIX`, or `REPLAN`, requiring it to apply the relevance gate, stop once it can determine the verdict, and omit optional or speculative findings; `PASS` ends the loop immediately; for `FIX`, group blocking findings by root cause, make one coherent correction, and retest; for `REPLAN`, stop patching, choose and implement one simpler proportionate mechanism, reconsulting Astra-xhigh only if genuinely new architectural advice is required, and retest; after either correction path, spawn exactly one fresh Astra-medium terminal final reviewer and require exactly `PASS` if the core problem and acceptance contract are satisfied or `UNRESOLVED` with only concrete remaining blockers and any missing evidence or user guidance if they are not; after this final review, stop, marking the task complete on `PASS` or reporting the unresolved state on `UNRESOLVED`, and never perform a second reviewer-triggered correction pass or spawn a third implementation reviewer unless a new user instruction starts a new cycle."
    }
  }
}
